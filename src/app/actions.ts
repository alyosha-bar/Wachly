"use server";
import { db } from '@/db';
// import { InsertPost, postsTable, usersTable } from "@/db/schema";

// export default async function createPost(data: InsertPost) {
//     await db.insert(postsTable).values(data).execute();
//   }